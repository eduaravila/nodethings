<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        link(rel="stylesheet" 
        href="/css/main.css")
        block styles
        title #{tituloPagina}
    body
        header.main-header
            nav.main-header-item--nav
                ul.nav-item--list
                    li.list-item
                        a(href="admin/agregarProducto" class=(activeUrl === "/agregarProducto" ? 'active':'')) Agregar un nuevo producto
                    li.list-item
                        a(href="/productos" class=(activeUrl === "/productos" ? 'active':'')) Lista de productos
    block contenido